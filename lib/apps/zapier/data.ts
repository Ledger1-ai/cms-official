export async function fetchZapierData(credentials: any) {
    // Zapier API interaction (usually webhooks, but fetching status here)
    return {
        webhooksActive: 5,
        triggersToday: 124
    };
}
